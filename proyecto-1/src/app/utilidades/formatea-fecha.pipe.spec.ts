import { FormateaFechaPipe } from './formatea-fecha.pipe';

describe('FormateaFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateaFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
